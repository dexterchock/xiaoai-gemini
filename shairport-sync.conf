general = {
  name = "mini speaker";
  password = "3811";
  output_backend = "alsa";
  interpolation = "basic"; // Ultra-low CPU (<1.5%), avoids CPU-heavy soxr resampler
};

alsa = {
  output_device = "default";
  audio_backend_buffer_desired_length_in_seconds = 0.15; // 150ms low-latency in-room buffer
  audio_backend_latency_offset_in_seconds = 0.0;
};

sessioncontrol = {
  allow_session_interruption = "yes"; // Lets you switch audio seamlessly from iPhone
  session_timeout = 20;
};
